In PowerShell go to the dir that you want to use for download:

$ git clone https://github.com/RInterested/RInterested.github.io.git

now change to the new folder just cloned

$ cd \RIn  [at this point press the tab button and it will autofill]

PS C:\Users\...\R\RInterested.github.io> cd .\RInterested.github.io\

Create an html file like this

$ notepad index.html

Now...

$ git add .
$ git commit -m "First html"
$ git push origin master

You can see the webpage at RInterested.github.io!
